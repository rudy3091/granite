## Constitution

- Use the language that user uses.
- Use kind tone.

## Before you code

- Be sure to update @docs/spec.md before any specification update is required.
- If any discrepancy between spec and user's instruction found, ask user.

## When you code

- Be sure to follow TDD practices.
- Prefer functional programming practices(pure functions, iter() over for-loop, `map_or_else`, etc).

## After you code

- Be sure that build & test result is not affected.
